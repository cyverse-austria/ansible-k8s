{
  "CN": "etcd",
  "hosts": [
    "localhost",
    "127.0.0.1",
    {% for host in groups['etcd-nodes'] %}
    "{{ hostvars[host]['ansible_hostname'] }}"{% if not loop.last %},{% endif %}
    {% endfor %}
  ],
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "AT",
      "L": "Vienna",
      "O": "Kubernetes",
      "OU": "ETCD-CA",
      "ST": "Vienna"
    }
  ]
}
